@model IEnumerable<Gaming.Models.Place>

@{
    ViewBag.Title = "Index";
}

<h2>Places</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            Level Required
            <!--@Html.DisplayNameFor(model => model.Level_required)-->
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Level_required)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Place_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.Place_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Place_ID })
        </td>
    </tr>
}

</table>
